version: "3.8"

services:
  dashboard-app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: dashboard-app
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
